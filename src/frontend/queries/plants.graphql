query getPlants {
  plants {
    id
    name
    description
    imageUrl
    moduleConfigs {
      moduleType
      rangeFrom
      rangeTo
    }
  }
}

query getPlantById($id: UUID!) {
  plant(id: $id) {
    id
    name
    description
    imageUrl
    moduleConfigs {
      moduleType
      rangeFrom
      rangeTo
    }
  }
}
